/**
 * @typedef { import("../../main.js").Config } Config
 *
 * @typedef { [number, number] } Point
 */

/**
 * @param { Config } config
 * @param { Point[] } allocation
 * @returns { Point[] }
 */
export default function dummy(config, allocation) {
  return allocation;
}
